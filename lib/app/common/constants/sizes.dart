// Font Sizes
import 'package:flutter/material.dart';

const headlineLargeSize = 32.0;
const headlineMediumSize = 24.0;
const headlineSmallSize = 18.0;
const titleLargeSize = 16.0;
const titleMediumSize = 16.0;
const titleSmallSize = 16.0;
const bodyLargeSize = 14.0;
const bodyMediumSize = 14.0;
const bodySmallSize = 14.0;
const labelLargeSize = 12.0;
const labelMediumSize = 12.0;

// Dynamic sizes
screenHeight(context) => MediaQuery.sizeOf(context).height;
screenWidth(context) => MediaQuery.sizeOf(context).width-15;

// Card sizes
const smallCardRadius = 12;



// Image Sizes
const double logoImageSize = 60.0;


// Spacers
// ignore: constant_identifier_names
const spacer = SizedBox(height: 10.0,);
const titleBodyVerticalSpacer = SizedBox(height: 10.0,);
const inputFieldsVerticalSpacer = SizedBox(height: 10.0,);
const inputFieldsHorizontalSpacer = SizedBox(width: 10.0,);
const bodyButtonsVerticalSpacer = SizedBox(height: 12.0,);

